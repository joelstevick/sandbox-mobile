// NOTE object below must be a valid JSON
window.sandbox_mobile = $.extend(true, window.sandbox_mobile, {
  "config": {
    "layoutSet": "navbar",
    "navigation": [
      {
        "title": "About",
        "onExecute": "#about",
        "icon": "info"
      }
    ]
  }
});